# Travaux pratiques - Fondement de l'IA

## Dependencies

- make
- g++

## Compile and run the project

uses the `make` configuration to compile the project

```bash
make
```

You can also use the `clean` command to wipe the binaries.

```bash
make clean
```

To run the project :

```bash
./game
```
